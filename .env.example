# Локально (хост): порт 5433 проброшен из docker-compose
DATABASE_URL="postgresql://scrape:scrape@localhost:5433/scrape"

# Публичный базовый URL приложения (только не-секреты для демо)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Для POST /api/pipeline/run с source=mock-feed (серверный fetch), если отличается от NEXT_PUBLIC_APP_URL
# APP_BASE_URL="http://localhost:3000"

# Воркер (scripts/pipeline-worker.ts): выражение cron (5 полей), мок-источник
# PIPELINE_CRON="*/2 * * * *"
# MOCK_SOURCE=fixture | mock-feed
# MOCK_FEED_URL="http://host.docker.internal:3000/mock-feed"
